{
  "name": "@{{project_slug}}/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo build config",
    "dev": "echo dev config",
    "test": "echo test config"
  }
}
